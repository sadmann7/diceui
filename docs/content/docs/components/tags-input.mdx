---
title: Tags Input
description: Display a list of tags in an input field with the ability to add, edit, and remove them.
---


<ComponentTabs name="tags-input-demo" />


## Installation


```package-install
@diceui/tags-input
```

## Integratation with shadcn/ui


### CLI


```package-install
shadcn@latest add "https://diceui.com/r/tags-input"

```


### Manual

<Steps>
  <Step>
     Install the following dependencies:

     ```package-install
     @diceui/tags-input
     ```
  </Step>
  <Step>
    Copy and paste the following code into your project.

    <ComponentSource name="tags-input" />
  </Step>
</Steps>


## Examples


### Editable

<ComponentTabs name="tags-input-editable-demo" />

### With Sortable

`TagsInput` can be composed with [`Sortable`](/docs/components/sortable) to allow reordering of tags.

<ComponentTabs name="tags-input-sortable-demo" />

## API Reference

### Root

Container for the tags input.

<AutoTypeTable
  path="./types/docs/tags-input.ts"
  name="RootProps"
/>

<DataAttributesTable
  attributes={[
    {
      title: "[data-invalid]",
      value: "Present when invalid",
    },
    {
      title: "[data-disabled]",
      value: "Present when disabled",
    },
  ]}
/>

### Label

Label element for the tags input.

<AutoTypeTable
  path="./types/docs/tags-input.ts"
  name="LabelProps"
/>

<DataAttributesTable
  attributes={[
    {
      title: "[data-disabled]",
      value: "Present when the tags input is disabled",
    },
  ]}
/>

### Content

Container for the tags and input element.

<AutoTypeTable
  path="./types/docs/tags-input.ts"
  name="ContentProps"
/>

<DataAttributesTable
  attributes={[
    {
      title: "[data-disabled]",
      value: "Present when disabled",
    },
  ]}
/>


### Input

Text input for adding new tags.

<AutoTypeTable
  path="./types/docs/tags-input.ts"
  name="InputProps"
/>

<DataAttributesTable
  attributes={[
    {
      title: "[data-invalid]",
      value: "Present when the input value is invalid",
    },
    {
      title: "[data-disabled]",
      value: "Present when disabled",
    },
  ]}
/>

### Item

Individual tag item.

<AutoTypeTable
  path="./types/docs/tags-input.ts"
  name="ItemProps"
/>

<DataAttributesTable
  attributes={[
    {
      title: "[data-state]",
      value: ["active", "inactive"],
    },
    {
      title: "[data-highlighted]",
      value: "Present when the tag is highlighted",
    },
    {
      title: "[data-disabled]",
      value: "Present when the tag is disabled",
    },
    {
      title: "[data-editing]",
      value: "Present when the tag is being edited",
    },
    {
      title: "[data-editable]",
      value: "Present when the tags input is editable",
    },
  ]}
/>


### Text

Text content of a tag.

<AutoTypeTable
  path="./types/docs/tags-input.ts"
  name="ItemTextProps"
/>


### Delete

Button to remove a tag.

<AutoTypeTable
  path="./types/docs/tags-input.ts"
  name="ItemDeleteProps"
/>

<DataAttributesTable
  attributes={[
    {
      title: "[data-disabled]",
      value: "Present when the delete button is disabled",
    },
    {
      title: "[data-state]",
      value: "'active' when highlighted, 'inactive' when not",
    },
  ]}
/>


### Clear

Button to clear all tags.

<AutoTypeTable
  path="./types/docs/tags-input.ts"
  name="ClearProps"
/>

<DataAttributesTable
  attributes={[
    {
      title: "[data-disabled]",
      value: "Present when the clear button is disabled",
    },
    {
      title: "[data-state]",
      value: "'visible' when there are tags, 'hidden' when empty",
    },
  ]}
/>

## Accessibility

### Keyboard Interactions

<KeyboardShortcutsTable
  shortcuts={[
    {
      keys: ["Backspace", "Delete"],
      description: "When cursor is at start, removes the highlighted tag or the last tag if no tag is highlighted."
    },
    {
      keys: ["←", "→"],
      description: "Navigates between tags. When cursor is at start, moves focus to previous/next tag."
    },
    {
      keys: ["Home"],
      description: "When a tag is highlighted, moves focus to the first tag."
    },
    {
      keys: ["End"], 
      description: "When a tag is highlighted, moves focus to the last tag."
    },
    {
      keys: ["Enter"],
      description: "When input has text, adds a new tag. When a tag is highlighted and editable is enabled, enters edit mode."
    },
    {
      keys: ["Escape"],
      description: "Clears tag highlight and edit mode, resets cursor to start."
    },
    {
      keys: ["Tab"],
      description: "When `addOnTab` is enabled and input has text, adds a new tag."
    }
  ]}
/>
