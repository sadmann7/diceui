---
title: Timeline
description: A flexible timeline component for displaying chronological events with support for different orientations, RTL layouts, and visual states.
preview: true
links:
 api: /docs/components/timeline#api-reference
---

<ComponentTabs name="timeline-demo" />

## Installation

### CLI

```package-install
npx shadcn@latest add "https://diceui.com/r/timeline"

```

### Manual

<Steps>
  <Step>
     Install the following dependencies:

     ```package-install
     @radix-ui/react-slot class-variance-authority
     ```
  </Step>
  <Step>
    Copy and paste the following code into your project.

    <ComponentSource name="timeline" />
  </Step>
</Steps>

## Layout

```tsx
import * as Timeline from "@/components/ui/timeline";

<Timeline.Root>
  <Timeline.Item>
    <Timeline.Dot />
    <Timeline.Connector />
    <Timeline.Content>
      <Timeline.Header>
        <Timeline.Title>Event Title</Timeline.Title>
        <Timeline.Time>Date</Timeline.Time>
      </Timeline.Header>
      <Timeline.Description>Event description</Timeline.Description>
    </Timeline.Content>
  </Timeline.Item>
</Timeline.Root>
```

## Usage

### Basic Timeline

A simple vertical timeline showing chronological events.

```tsx
<Timeline.Root>
  <Timeline.Item completed>
    <Timeline.Dot />
    <Timeline.Connector />
    <Timeline.Content>
      <Timeline.Header>
        <Timeline.Title>Project Kickoff</Timeline.Title>
        <Timeline.Time dateTime="2024-01-15">January 15, 2024</Timeline.Time>
      </Timeline.Header>
      <Timeline.Description>
        Initial meeting with stakeholders to define project scope.
      </Timeline.Description>
    </Timeline.Content>
  </Timeline.Item>
  <Timeline.Item>
    <Timeline.Dot />
    <Timeline.Content>
      <Timeline.Header>
        <Timeline.Title>Development</Timeline.Title>
        <Timeline.Time dateTime="2024-03-01">March 1, 2024</Timeline.Time>
      </Timeline.Header>
      <Timeline.Description>
        Building the application with React and TypeScript.
      </Timeline.Description>
    </Timeline.Content>
  </Timeline.Item>
</Timeline.Root>
```

## Examples

### Horizontal Timeline

Display timeline events horizontally across the screen.

<ComponentTabs name="timeline-horizontal-demo" />

### RTL Timeline

Display timeline with right-to-left layout for RTL languages.

<ComponentTabs name="timeline-rtl-demo" />

## API Reference

### Root

The root container for timeline items.

<AutoTypeTable
  path="./types/docs/timeline.ts"
  name="RootProps"
/>

### Item

A single timeline item containing content, marker, and connector.

<AutoTypeTable
  path="./types/docs/timeline.ts"
  name="ItemProps"
/>

### Dot

The visual marker/dot for a timeline item.

<AutoTypeTable
  path="./types/docs/timeline.ts"
  name="DotProps"
/>

### Connector

The line connecting timeline items.

<AutoTypeTable
  path="./types/docs/timeline.ts"
  name="ConnectorProps"
/>

### Header

Container for the title and time of a timeline item.

<AutoTypeTable
  path="./types/docs/timeline.ts"
  name="HeaderProps"
/>

### Title

The title/heading of a timeline item.

<AutoTypeTable
  path="./types/docs/timeline.ts"
  name="TitleProps"
/>

### Description

The description/body text of a timeline item.

<AutoTypeTable
  path="./types/docs/timeline.ts"
  name="DescriptionProps"
/>

### Content

Container for the timeline item's content (header, description, etc.).

<AutoTypeTable
  path="./types/docs/timeline.ts"
  name="ContentProps"
/>

### Time

A semantic time element for displaying dates/times.

<AutoTypeTable
  path="./types/docs/timeline.ts"
  name="TimeProps"
/>

## Features

### Flexible Orientations

The timeline supports both vertical and horizontal orientations. Use the `orientation` prop on `Timeline.Root` to switch between layouts.

### RTL Support

The timeline fully supports right-to-left (RTL) layouts through the `dir` prop. When set to `"rtl"`, the timeline automatically flips its layout direction, making it ideal for RTL languages like Arabic, Hebrew, and Persian.

### Completed States

Control the visual state of individual timeline items using the `completed` prop. Completed items will be styled with the primary color to show progress.

```tsx
<Timeline.Root>
  <Timeline.Item completed>Step 1 - Completed</Timeline.Item>
  <Timeline.Item completed>Step 2 - Completed</Timeline.Item>
  <Timeline.Item>Step 3 - Current/Pending</Timeline.Item>
  <Timeline.Item>Step 4 - Not started</Timeline.Item>
</Timeline.Root>
```

### Custom Icons

Replace the default dot marker with custom icons or React components by passing children to `Timeline.Dot`, giving you full control over the visual appearance.

### Composition Pattern

Built with a composable API that gives you complete control over the structure and styling of your timeline. Mix and match components as needed.

## Accessibility

### Keyboard Support

The timeline is built with semantic HTML (`<ol>`, `<li>`, `<time>`) and supports standard keyboard navigation patterns.

### Semantic HTML

- Uses `<ol>` for the timeline root to represent ordered list of events
- Uses `<li>` for each timeline item
- Uses `<time>` with `dateTime` attribute for proper date representation
- Connectors are marked with `aria-hidden="true"` as they're purely decorative

## Notes

- The `Connector` component automatically hides on the last timeline item using CSS
- All components support the `asChild` prop for composition with other components
- The timeline adapts to your theme colors automatically through CSS variables
- The `dir` prop supports both `"ltr"` (default) and `"rtl"` for proper directional layout

