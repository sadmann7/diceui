---
title: Combobox
description: An input with a popover that helps users filter through a list of options.
preview: true
---

<ComponentTabs name="combobox-demo" />

## Installation

```package-install
@diceui/combobox
```

## Installation with shadcn/ui

### CLI

```package-install
npx shadcn@latest add "https://diceui.com/r/combobox"
```

### Manual

<Steps>
  <Step>
     Install the following dependencies:

     ```package-install
     @diceui/combobox
     ```
  </Step>
  <Step>
    Copy and paste the following code into your project.

    <ComponentSource name="combobox" />
  </Step>
</Steps>

## Anatomy

Import the parts, and compose them together.

```tsx
import * as Combobox from "@diceui/combobox";

<Combobox.Root>
  <Combobox.Label />
  <Combobox.Anchor>
    <Combobox.Input />
    <Combobox.Trigger />
    <Combobox.Cancel />
  </Combobox.Anchor>
  <Combobox.Portal>
    <Combobox.Content>
      <Combobox.Arrow />
      <Combobox.Empty />
      <Combobox.Group>
        <Combobox.GroupLabel />
        <Combobox.Item>
          <Combobox.ItemText />
          <Combobox.ItemIndicator />
        </Combobox.Item>
      </Combobox.Group>
      <Combobox.Separator />
    </Combobox.Content>
  </Combobox.Portal>
</Combobox.Root>
```

## Examples

### With Groups

<ComponentTabs name="combobox-groups-demo" />

### With Multiple Selection

<ComponentTabs name="combobox-multiple-demo" />

### With Custom Filter

<ComponentTabs name="combobox-custom-filter-demo" />

### With Virtualization

<ComponentTabs name="combobox-virtualized-demo" />

## API Reference

### Root

The main container for the combobox. Manages state, keyboard interactions, and provides context for all other combobox components.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="RootProps"
/>

<DataAttributesTable
  attributes={[
    {
      title: "[data-state]",
      value: ["open", "closed"],
    },
    {
      title: "[data-disabled]",
      value: "Present when disabled.",
    },
  ]}
/>

### Label

An accessible label that describes the combobox. Associates with the input element for screen readers.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="LabelProps"
/>

### Anchor

A wrapper element that positions the combobox popover relative to the input and trigger. Provides the reference point for floating positioning.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="AnchorProps"
/>

### Trigger

A button that toggles the combobox popover. Handles focus management and keyboard interactions for opening/closing the popover. Can be styled with custom icons or indicators.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="TriggerProps"
/>

<DataAttributesTable
  attributes={[
    {
      title: "[data-state]",
      value: ["open", "closed"],
    },
    {
      title: "[data-disabled]",
      value: "Present when disabled",
    },
  ]}
/>

### Input

The text input field that users can type into to filter options. Supports controlled and uncontrolled modes, with built-in filtering capabilities.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="InputProps"
/>

### Cancel

A button that clears the input value and resets the filter. Automatically appears when the input has a value, unless `forceMount` is enabled. Returns focus to the input after clearing.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="CancelProps"
/>

### Portal

Creates a portal for rendering the combobox content outside of its DOM hierarchy. Ensures proper stacking context and z-index behavior.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="PortalProps"
/>

### Content

The floating container for combobox options. Features smart positioning with collision detection, boundary constraints, and responsive sizing. Supports custom animations and transitions.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="ContentProps"
/>

<DataAttributesTable
  attributes={[
    {
      title: "[data-state]",
      value: ["open", "closed"],
    },
    {
      title: "[data-side]",
      value: ["top", "right", "bottom", "left"],
    },
    {
      title: "[data-align]",
      value: ["start", "center", "end"],
    },
  ]}
/>

<CSSVariablesTable
  variables={[
    {
      title: "--dice-transform-origin",
      description: "Transform origin for anchor positioning.",
    },
    {
      title: "--dice-anchor-width",
      description: "Width of the anchor element.",
    },
    {
      title: "--dice-anchor-height",
      description: "Height of the anchor element.",
    },
    {
      title: "--dice-available-width",
      description: "Available width in the viewport for the floating element.",
    },
    {
      title: "--dice-available-height",
      description: "Available height in the viewport for the floating element.",
    },
  ]}
/>

### Arrow

A visual arrow element that points to the anchor. Automatically positions and rotates based on the content placement. Supports custom styling and dimensions.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="ArrowProps"
/>

### Empty

A placeholder component displayed when no options match the current filter. Can be customized with helpful messages or suggestions.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="EmptyProps"
/>

### Group

A container for logically grouping related options. Can be used with `GroupLabel` to create accessible groupings.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="GroupProps"
/>

### GroupLabel

A label that describes a group of options. Provides context and improves accessibility for grouped options.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="GroupLabelProps"
/>

### Item

An interactive option in the combobox list. Handles selection, highlighting, and keyboard navigation.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="ItemProps"
/>

<DataAttributesTable
  attributes={[
    {
      title: "[data-highlighted]",
      value: "Present when the item is highlighted",
    },
    {
      title: "[data-disabled]",
      value: "Present when the item is disabled",
    },
    {
      title: "[data-state]",
      value: ["checked", "unchecked"],
    },
  ]}
/>

### ItemText

The textual content of an item. Should only contain the text content without additional markup.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="ItemTextProps"
/>

### ItemIndicator

A visual indicator for selected options. Can be customized with icons or styles to show the selection state.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="ItemIndicatorProps"
/>

### Separator

A visual divider for separating options or groups. Improves visual hierarchy and organization of options.

<AutoTypeTable
  path="./types/docs/combobox.ts"
  name="SeparatorProps"
/>

## Accessibility

### Keyboard Interactions

<KeyboardShortcutsTable
  shortcuts={[
    {
      keys: ["Enter"],
      description: "When open, selects the highlighted option."
    },
    {
      keys: ["↑", "↓"],
      description: "When open, moves focus to the previous/next option."
    },
    {
      keys: ["Home", "End"],
      description: "When open, moves focus to the first/last option."
    },
    {
      keys: ["Page Up", "Page Down"],
      description: "When open and modal is enabled, moves focus to the previous/next page of options."
    },
    {
      keys: ["Escape"],
      description: "Closes the combobox popover."
    },
  ]}
/> 