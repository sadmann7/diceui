---
title: Time Picker
description: An accessible time picker component with inline editing and dropdown selection. Automatically adapts to 12-hour or 24-hour format based on user locale.
preview: true
links:
  api: /docs/components/time-picker#api-reference
---

<ComponentTabs name="time-picker-demo" />

## Installation

### CLI

```package-install
npx shadcn@latest add "https://diceui.com/r/time-picker"
```

### Manual

<Steps>
  <Step>
     Install the following dependencies:

     ```package-install
     @radix-ui/react-slot
     ```
  </Step>
  <Step>
    Copy the refs composition utilities into your `lib/compose-refs.ts` file.

    <ComponentSource name="compose-refs" />
  </Step>
  <Step>
    Copy the visually hidden input component into your `components/visually-hidden-input.tsx` file.

    <ComponentSource name="visually-hidden-input" />
  </Step>
  <Step>
    Copy and paste the following code into your project.

    <ComponentSource name="time-picker" />
  </Step>
</Steps>

## Layout

Import the parts, and compose them together. Like the native HTML time picker, the inputs are always visible and the trigger opens a dropdown for easier selection.

```tsx
import { Clock } from "lucide-react";
import * as TimePicker from "@/components/ui/time-picker";

return (
  <TimePicker.Root>
    <TimePicker.Label />
    {/* Inline inputs - always visible, like native time picker */}
    <div className="relative flex items-center">
      <div className="flex h-10 w-full items-center gap-1 rounded-md border border-input bg-background px-3 py-2 pr-10">
        <TimePicker.Input segment="hour" />
        <TimePicker.Separator />
        <TimePicker.Input segment="minute" />
      </div>
      {/* Trigger opens the dropdown */}
      <TimePicker.Trigger className="absolute right-0 flex h-full items-center px-3 hover:bg-accent">
        <Clock className="size-4" />
      </TimePicker.Trigger>
    </div>
    {/* Dropdown content */}
    <TimePicker.Content>
      <TimePicker.Hour />
      <TimePicker.Minute />
      <TimePicker.Clear />
    </TimePicker.Content>
  </TimePicker.Root>
)
```

## Examples

### With Step

Use the `hourStep`, `minuteStep`, and `secondStep` props to set custom intervals for hour, minute, and second selection respectively.

<ComponentTabs name="time-picker-step-demo" />

### With Seconds

Include seconds in time selection.

<ComponentTabs name="time-picker-seconds-demo" />

### Controlled State

Control the time picker value programmatically.

<ComponentTabs name="time-picker-controlled-demo" />

### With Form

Use the time picker in a form with validation.

<ComponentTabs name="time-picker-form-demo" />

## API Reference

### Root

The main container component for time picker functionality.

<AutoTypeTable
  path="./types/docs/time-picker.ts"
  name="RootProps"
/>

<DataAttributesTable
  data={[
    {
      title: "[data-disabled]",
      value: "Present when the time picker is disabled",
    },
    {
      title: "[data-invalid]",
      value: "Present when the time picker is invalid",
    },
    {
      title: "[data-readonly]",
      value: "Present when the time picker is read-only",
    },
  ]}
/>

### Label

The label component for the time picker field.

<AutoTypeTable
  path="./types/docs/time-picker.ts"
  name="LabelProps"
/>

### Trigger

Button to open the time picker dropdown.

<AutoTypeTable
  path="./types/docs/time-picker.ts"
  name="TriggerProps"
/>

<DataAttributesTable
  data={[
    {
      title: "[data-state]",
      value: "'open' | 'closed'",
    },
    {
      title: "[data-disabled]",
      value: "Present when the time picker is disabled",
    },
    {
      title: "[data-readonly]",
      value: "Present when the time picker is read-only",
    },
    {
      title: "[data-invalid]",
      value: "Present when the time picker is invalid",
    },
  ]}
/>

### Content

Container for the time selection interface.

<AutoTypeTable
  path="./types/docs/time-picker.ts"
  name="ContentProps"
/>

<DataAttributesTable
  data={[
    {
      title: "[data-state]",
      value: "'open' | 'closed'",
    },
    {
      title: "[data-side]",
      value: "The side where the content is positioned",
    },
    {
      title: "[data-align]",
      value: "The alignment of the content",
    },
  ]}
/>

### Hour

Component for selecting hours.

<AutoTypeTable
  path="./types/docs/time-picker.ts"
  name="HourProps"
/>

### Minute

Component for selecting minutes.

<AutoTypeTable
  path="./types/docs/time-picker.ts"
  name="MinuteProps"
/>

### Second

Component for selecting seconds.

<AutoTypeTable
  path="./types/docs/time-picker.ts"
  name="SecondProps"
/>

### Period

Component for selecting AM/PM in 12-hour format.

<AutoTypeTable
  path="./types/docs/time-picker.ts"
  name="PeriodProps"
/>

### Separator

Visual separator between time units.

<AutoTypeTable
  path="./types/docs/time-picker.ts"
  name="SeparatorProps"
/>

### Clear

Button to clear the selected time.

<AutoTypeTable
  path="./types/docs/time-picker.ts"
  name="ClearProps"
/>

### Input

Inline editable input field for time segments (hour, minute, second, period).

<AutoTypeTable
  path="./types/docs/time-picker.ts"
  name="InputProps"
/>

## Accessibility

### Keyboard Interactions

<KeyboardShortcutsTable
  data={[
    {
      keys: ["Space"],
      description: "Opens/closes the time picker when trigger is focused",
    },
    {
      keys: ["Enter"],
      description: "Opens/closes the time picker when trigger is focused. Confirms input editing.",
    },
    {
      keys: ["Escape"],
      description: "Closes the time picker or cancels input editing",
    },
    {
      keys: ["Tab"],
      description: "Moves focus to the next focusable element",
    },
    {
      keys: ["Shift", "Tab"],
      description: "Moves focus to the previous focusable element",
    },
    {
      keys: ["ArrowUp"],
      description: "When in dropdown: Navigate to previous option. When in input: Increment value.",
    },
    {
      keys: ["ArrowDown"],
      description: "When in dropdown: Navigate to next option. When in input: Decrement value.",
    },
    {
      keys: ["A/P"],
      description: "When focused on period input: Toggle between AM/PM",
    },
  ]}
/>

## Notes

- **Native-like behavior**: Works like the HTML time input - click segments to select and edit, use arrow keys to adjust
- **Inline editing**: All time segments are always visible and editable, no need to open the dropdown
- **Dropdown for convenience**: The clock icon trigger opens a dropdown for easier selection
- **Automatic format detection**: Display format (12-hour vs 24-hour) is automatically detected from user's locale settings
- **Consistent value format**: The time value is always stored in 24-hour format ("HH:mm" or "HH:mm:ss"), regardless of display format
- **Locale override**: Use the `locale` prop to explicitly set a locale (e.g., "en-US" for 12-hour, "en-GB" for 24-hour)
- The `minuteStep`, `hourStep`, and `secondStep` props allow customization of selection intervals
- Use the `min` and `max` props to restrict the selectable time range
- **Period editing**: When using 12-hour format, focus on the period (AM/PM) and press 'a' or 'p' to change it
