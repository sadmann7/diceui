{
  "name": "diceui",
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=22.13.0"
  },
  "private": true,
  "type": "module",
  "workspaces": [
    "docs",
    "packages/*"
  ],
  "scripts": {
    "clean": "rimraf --glob **/node_modules **/dist **/.source **/.turbo **/.next **/pnpm-lock.yaml **/.tsbuildinfo",
    "clean:cache": "rimraf --glob **/.turbo **/.next/cache **/node_modules/.cache **/node_modules/.vite",
    "build": "turbo build",
    "build:registry": "pnpm --filter=docs build:registry --loglevel silent",
    "validate:registry": "pnpm --filter=docs validate:registry --loglevel silent",
    "clean:registry": "pnpm --filter=docs clean:registry --loglevel silent",
    "test:registry": "pnpm --filter=docs test:registry --loglevel silent",
    "build:packages": "pnpm --filter \"@diceui/*\" build",
    "dev": "turbo dev",
    "docs:dev": "turbo -F docs dev",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt --write",
    "format:check": "oxfmt --check",
    "typecheck": "turbo typecheck",
    "check": "turbo lint typecheck --affected && pnpm format",
    "test": "turbo test",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@testing-library/jest-dom": "catalog:",
    "@testing-library/react": "catalog:",
    "@testing-library/user-event": "catalog:",
    "@types/node": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "expect-type": "^1.4.0",
    "jsdom": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "rimraf": "^6.1.3",
    "turbo": "^2.10.12",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-tsconfig-paths": "^6.1.1",
    "vitest": "catalog:"
  }
}
